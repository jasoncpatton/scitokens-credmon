This directory is utilized by the HTCondor credd, condor_starter,
condor_oauth_credmon, and the web services associated with the condor_oauth_credmon
in order to manage tokens.

Each user or job with active tokens is represented by a sub-directory inside
this location; the HTCondor parameter SEC_CREDENTIAL_DIRECTORY_OAUTH should point
at this location.
